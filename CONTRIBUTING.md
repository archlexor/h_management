# Contributing

## Getting Started

1. Fork or branch from `main`.
2. Install dependencies with `bun install`.
3. Run `bun run lint` and `bun run test` before opening a pull request.

## Pull Requests

- Keep changes small and focused.
- Include screenshots for UI changes when relevant.
- Call out any follow-up work in the PR description.